export type newImage = {
  id: string;
  url: string;
  display_url: string;
}

export type Question = {
  id: number;

    img?: string;
    value?: string;
    title?: string;
    size?: string
  
};